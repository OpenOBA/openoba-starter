import { AliyunSmsService } from './aliyun-sms.service'

describe('AliyunSmsService', () => {
  it('should be defined', () => {
    const service = new AliyunSmsService()
    expect(service).toBeDefined()
  })
})
